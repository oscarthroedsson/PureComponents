{
  "name": "pure",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "packageManager": "pnpm@10.30.1",
  "scripts": {
    "ui": "pnpm --filter dev-shell",
    "components": "pnpm --filter purecomponents",
    "dev": "pnpm --filter dev-shell dev",
    "build": "pnpm --filter dev-shell build",
    "preview": "pnpm --filter dev-shell preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.3"
  }
}
