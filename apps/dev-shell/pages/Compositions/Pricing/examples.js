/* Pricing example registry. Keep this file metadata-only: the viewer loads
   each source when its section approaches the viewport. */

window.PC_PRICING_EXAMPLES = [
  {
    id: "plan-comparison",
    title: "Plan comparison",
    description:
      "Three plans compared feature by feature. It is a real table: plans are column headers, features are row headers, and each section is its own tbody. Table, Button, Tabs, Tooltip, Box and Layout carry it; the page only sets how wide the section is.",
    module: "./Pricing/examples/plans.js",
    height: 1200,
  },
];
