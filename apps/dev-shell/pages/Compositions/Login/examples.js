/* Login example registry. Keep this file metadata-only: the viewer loads
   each source when its section approaches the viewport. */

window.PC_LOGIN_EXAMPLES = [
  {
    id: "login-card",
    title: "Login card",
    description:
      "A sign-in card: email, password with a reset link, a primary action, a provider button and a sign-up line. Built only from Card, Form, Input, Label, Button, Link, Box, Layout and Behavior.",
    module: "./Login/examples/login-card.js",
    height: 560,
  },
];
