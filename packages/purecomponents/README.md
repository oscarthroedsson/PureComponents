# PureComponents

Component layer built on top of [PureUI](../pureui). Vanilla TS first, then
React, possibly Vue.

**Not started.** Nothing here yet — the package exists so the monorepo has a
place to put it. `private: true` until there is something worth publishing.
